package com.javaide.mobile.practice.model

/** A worked input/output example shown to the user, e.g. in a challenge's problem statement. */
data class ExerciseExample(
    val input: String,
    val output: String,
    val explanation: String
)
